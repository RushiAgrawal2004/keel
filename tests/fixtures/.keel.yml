version: 1

project:
  name: guide-fixtures

graph:
  provider: graphify
  path: sample_graph.json

layers:
  UI:
    - src/components
  SERVICE:
    - src/services
  DATABASE:
    - src/db

rules:
  - forbid:
      from: UI
      to: DATABASE
  - no_cycles: true

